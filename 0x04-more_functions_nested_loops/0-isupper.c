#include "main.h"

/**
 * _isupper - checks if int is uppercase
 *
 * Return: 1 (success) and 0 (failure)
 */

int _isupper(int c)
{
	int a;

	for (a = 65; a < 100; a++)
	{
		if (c == a)
		{
			Return (1);
		}
	}
	return (0);
}
