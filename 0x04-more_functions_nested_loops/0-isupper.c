#include "main.h"

/**
 * _isupper - checks if int is uppercase
 *
 * Return: 1 (success) and 0 (failure)
 */

int _isupper(int c)
{
	int a;

	if (a >= 65 && a <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
