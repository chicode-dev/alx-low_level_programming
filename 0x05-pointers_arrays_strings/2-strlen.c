#include "main.h"

/**
 * _strlen - length of string
 * @s: string parameter
 * Return: string length
 */

int _strlen(char *s)
{
	int count = 0;

	while (*s != '\0')
	{
		/* increase number */
		count++
		/* increase char value */
		s++
	}
	return count;
}
