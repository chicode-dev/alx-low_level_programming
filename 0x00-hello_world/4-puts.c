#include <stdio.h>
#include <string.h>
/**
 *using puts function
 *using betty style
 *not a good style
 */
int main(void) {
  char str[] = "\"Programming is like building a multilingual puzzle";
  puts(str);
  return (0);
}
