#include <stdio.h>
#include <string.h>

/**
 *using puts function
 *using betty style
 *not a good style
 *
 * main - Entry point
 *
 * return: Always 0 (Success)
 */

int main(void)
{
  char str[] = "\"Programming is like building a multilingual puzzle";
  puts(str);
  return (0);
}
